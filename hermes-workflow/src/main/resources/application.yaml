server:
  port: 9090

spring:
  datasource:
    driver-class-name: com.ibm.as400.access.AS400JDBCDriver
    url: jdbc:as400://${AS400_SYSTEM:system}/bib;translate binary=true
    username: ${DB_USERNAME:user}
    password: ${DB_PASSWORD:pass}

  freemarker:
    template-loader-path: classpath:/templates/
    suffix: .ftl
    charset: UTF-8
    check-template-location: true

workflow:
  hot-folder: ${HOT_FOLDER:${user.dir}/hotfolder}
  processed-folder: ${PROCESSED_FOLDER:${user.dir}/processed}
  executeSql:
    defaultQuery: "SELECT 'Fallback' AS Wert FROM SYSIBM.SYSDUMMY1"
  applyTemplate:
    subjectTemplate: classpath:/templates/emailSubject.ftl
    bodyTemplate: classpath:/templates/emailBody.ftl
  transform:
    type: csv

export:
  xslt-path: classpath:/xslt
  file-path: ${EXPORT_DIR:classpath:/export}
  formats: html, json, jsonl, csv, md

as400:
  system: ${AS400_SYSTEM:YOUR_SYSTEM}
  username: ${AS400_USERNAME:YOUR_USER}
  password: ${AS400_PASSWORD:YOUR_PWD}

dataqueue:
  library: ${QUEUE_LIBRARY:YOUR_LIB}
  rpg2java: RP2JQ
  java2rpg: J2RPQ
  max-entry-length: 512
  description: "Java-RPG Integration Queue"

mail:
  host: ${MAIL_HOST:smtp.example.com}
  port: ${MAIL_PORT:587}
  username: ${MAIL_USERNAME:user}
  password: ${MAIL_PASSWORD:pass}
  from: noreply@example.com
  properties:
    mail.smtp.auth: true
    mail.smtp.starttls.enable: true

encryption:
  key: ${ENCRYPTION_KEY:mySuperSecretKey0815}

fallback:
  recipients: "my.mail@localhost.local"